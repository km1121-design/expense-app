# 変更履歴

このファイルは、まとまった単位のリリースを記録します。
新しいものを上に追記してください。

## 1.3.0 — 2026-08-14

PL管理モデル（別スプレッドシート）への経費連携を追加した。
`PL_SPREADSHEET_ID` を設定しない限り一切動かないため、既存の動作は変わらない。

- **承認済みの経費申請を『経費入力テーブル』へ自動反映**するようにした。
  申請の保存・承認・却下・削除に追随し、PL計算シートの SUMIFS がそのまま拾う。
  却下・差戻し・削除ではPLから取り下げる。

- **日付を実日付で書くようにした。** PL側の SUMIFS は `">=2026-08-01"` と日付として
  比較するため、日付列が文字列だと一件も一致しない。連携前のモデルが全月 ¥0
  だったのはこれが原因で、`setupPlSheets` が既存の文字列日付もまとめて移行する。

- **語彙の変換を「PL連携マッピング」シートに出した。** アプリ（交通費／本部）と
  PL（雑費交通費／イベント営業）で言葉が違うため、そのままでは集計されない。
  既定では人材以外の事業部を「イベント営業」へ寄せる。運用側で直せる。

- **手動入力行を壊さないようにした。** PL側の表はアプリ連携と手動入力が混在する
  設計なので、G列『申請ID』でアプリ由来の行だけを見分けて upsert する。
  申請IDが空の行は読み取りも書き換えもしない。

- **経費を取りこぼさないようにした。** PL側に行が無い科目（会議費・消耗品費・
  通信費・宿泊費・その他）は「その他経費（実費）」行で受ける。この行は
  「事業部×月の経費合計 − 個別行で拾っている分」の差で出しているため、
  今後アプリ側に科目が増えてもPLから漏れない。
  対応先の無い**事業部**だけは金額の行き先を決められないので書かず、
  「PL連携スキップ」へ理由を残して管理者ダッシュボードに警告を出す
  （対応表を直して「今すぐ再同期」で解消できる）。

- SUMIFS の範囲を 100 行から 2000 行へ広げた（元は実質96件で打ち止めだった）。

- メンテナンス関数: `setupPlSheets`（PL側の初期設定・冪等）/
  `syncAllToPl`（既存申請の一括反映・埋め戻し）。API に `syncPl`（admin）を追加。

## 1.2.2 — 2026-08-14

CIの保守のみ。アプリの動作は変わらない。

- **GitHub Actions を現行のメジャーへ更新**し、Node 20 の非推奨警告を解消した。
  警告はアクション自身のランタイムについてのもので、1.1.0 で `node-version` を
  22 へ上げたのとは別の話（そちらはワークフローの各ステップが使う Node）。

  | アクション | 前 | 後 |
  | --- | --- | --- |
  | `actions/checkout` | v4 | v7 |
  | `actions/setup-node` | v4 | v7 |
  | `actions/configure-pages` | v5 | v6 |
  | `actions/upload-pages-artifact` | v3 | v5 |
  | `actions/deploy-pages` | v4 | v5 |

  `upload-pages-artifact` は v4 以降、**隠しファイル（ドットファイル）を既定で
  含めない**。`expense-app/` に隠しファイルは無いため既定のままとした。
  今後 `.nojekyll` などを置く場合は `include-hidden-files: true` が必要になる。

## 1.2.1 — 2026-08-14

申請（保存）の待ち時間を削った。1.2.0 と同じ方針の続きで、機能の変更はない。

### アプリ側

- **保存後に一覧を取り直すのをやめた**。`create` は保存済みのレコードを返すのに、
  それを捨てて `refreshFromCloud()` で全件を取得し直していた。往復が2回になり、
  申請のたびに全期間の申請をダウンロードしていた。返ってきたレコードを
  そのまま一覧へ加える（保存結果を返さない古いバックエンドへは従来動作で切り替え）。

計測（既存30件・API 800ms/回・画像なし）:
申請完了まで **1,694ms → 867ms**、往復 2回 → **1回**。

### バックエンド

- **領収書ファイル名の採番で、月フォルダ全体を走査するのをやめた**。
  「同じ日・同じ申請者」でドライブ側を検索してから数える。従来はその月の
  申請が増えるほど保存が遅くなっていた。
- **1リクエスト内でユーザーシートを2回読むのをやめた**。`requireUser_` が
  読み取ったユーザー行の `department` を使い回す（`create` / `me` / `bootstrap`）。
- **未登録の区間を運賃マスタへ追記するとき、マスタを2度読むのをやめた**。
  不在は確認済みなので `appendFare_` で追記のみ行う。

## 1.2.0 — 2026-08-14

起動・同期・AI解析の待ち時間を削った。機能の追加や削除はしていない。

計測したところ、アプリ自身のJS実行は約200msで、待ち時間はほぼすべて
バックエンドへの往復とCDNの読み込みだった（申請20件・API 800ms/回の条件で
一覧が出るまで2,496ms、モバイル回線相当のCDN遅延を足すと5,466ms）。

### 起動

- **OCRライブラリ（Tesseract.js）を遅延読み込みに変更**。`index.html` で同期読み込み
  していたため、AIキーが設定されていて一度も使わない運用でも、毎回その通信が
  終わるまでアプリのJSが動かなかった。実際に必要になった時点で読み込む。
- **起動を1往復に統合**（`bootstrap` アクション）。`status` → `me` → 一覧取得の
  3往復を1つにまとめた。バックエンドが古い場合は従来の3往復へ自動で切り替わる。
- **通信の前にキャッシュを描画**。クラウドモードの正常系でキャッシュを読んでおらず、
  3往復が終わるまで白い画面だった。先に前回の内容を描き、応答が返ったら差し替える。

計測（申請20件・API 800ms/回・CDN 3000ms）:
一覧が出るまで **5,466ms → 342ms**（キャッシュあり）／**957ms**（初回・キャッシュなし）。
往復回数 3回 → 1回。起動時のCDN取得なし。

### 一覧表示

- **領収書画像をリンク共有に設定**（`shareReceiptFile_`）。画像はスクリプト所有者の
  ドライブにあるため、共有しないと所有者以外はサムネイルの直リンクが403になり、
  画面に出ている枚数だけアプリ経由の取得が走っていた（1枚＝1往復）。
  所有者本人だけは直リンクが通るため、この遅さは管理者から見えていなかった。
  - **URLを知っていれば認証なしで画像を閲覧できる状態になる。** 許容できない運用では
    `shareReceiptFile_` の中身を空にすれば従来の挙動（遅いが所有者に閉じる）へ戻せる。
  - 既存の画像は、エディタで `shareExistingReceipts` を一度実行すると揃う。
- **`receiptImage` の認可でシート全体を読むのをやめた**。画像1枚ごとに経費シートを
  全読みしていたのを、対象の1列だけを引いて該当行を特定する方式に変更。

### AI画像解析

- **書き起こし（`raw_text`）を根拠行だけに縮小**。応答時間は出力トークン数でほぼ
  決まるため、全文転記から店名・発行日・合計金額の行を中心とした最大6行
  （`RECEIPT_RAW_TEXT_MAX_LINES`）に絞った。精度が落ちる場合はこの定数を増やす。
- **読み直しの条件を厳しくした**。これまでは誤読履歴がある店舗というだけで毎回
  2回AIを呼んでおり、よく使う店舗ほど遅くなっていた。今回の読み取りが過去の
  誤読と同じ値を出したときだけ読み直す（`repeatsKnownMistake_`）。
  違う値なら読み直しても直せる根拠が無いため呼ばない。

## 1.1.0 — 2026-08-14

運賃マスタを事前に用意しなくても、運用しながら区間が貯まるようにした。

### 変更

- **申請した区間を運賃マスタへ自動登録する**: 未登録の区間が申請されると、
  申請額から片道運賃を割り戻して運賃マスタへ登録する
  （`片道運賃 = 申請額 ÷ (往復なら2) ÷ 回数`）。2回目以降の同じ区間は、
  これまでどおり運賃マスタの値で自動照合される。上りと下りは同じ区間として扱う。
  - 登録の元になった申請は「一致」ではなく **「新規登録」** として残す。
    自分の申請額で自分を照合しても確認したことにならないため、管理者が承認前に
    金額を確かめられるようにしている。運賃マスタ側にも「申請（申請者名）」と記録する。
  - 管理者ダッシュボードの一覧に「🚃 新規登録」バッジを追加し、運賃照合の
    絞り込みからも選べるようにした。
  - **登録済みの区間は上書きしない**（申請額を変えるだけで照合の基準を動かせて
    しまうため）。差額はこれまでどおり「差額あり」として出る。
  - 割り切れない金額（運賃以外が混ざっている・回数が実態と合わない）、
    片道10万円を超える額（桁の打ち間違い）、金額0の申請は登録せず「未照合」のまま残す。
- 運賃マスタを事前に一括登録する運用は不要になった（「まとめて登録」は残している）。
- デプロイワークフローの Node.js を 20 から 22 へ上げた（20 の非推奨警告の解消）。

## 1.0.0 — 2026-08-14

レシート読み取りから承認までをブラウザで完結させる最初の安定版。
この版の内容はコミット `d5b79f2` 時点（Apps Script のデプロイはバージョン28）。

### 機能

- **レシート画像のAI解析**: 金額・日付・店名・科目・摘要を自動入力する。
  Gemini（無料枠可）と Claude に対応し、AIキー未設定時は端末内OCR
  （Tesseract.js）へフォールバックする。構造化出力を使うため応答は常にJSON。
- **手修正の学習**: 手直しした内容を店舗ごとに記憶し、次回同じ店舗のレシートで
  自動補正する。過去に誤読した店舗では、誤りをプロンプトに添えて1回だけ読み直す。
  モデルの再学習ではなく、学習ログの記録と再利用で実現している。
- **電車賃の照合**: 出発駅・到着駅・片道/往復・回数から想定運賃を出し、申請額と
  突き合わせる。調べた区間は運賃マスタに蓄積し、2回目以降は検索せず即照合する。
  判定はサーバー側で運賃マスタから再計算するため、申請者側では書き換えられない。
- **領収書ビューア**: 一覧に領収書サムネイルを表示し、クリックで拡大、← → で
  次の領収書へ送れる。カード表示と表表示を切り替えられる。
- **ログイン・権限**: ユーザーID＋パスワード認証（ソルト付きSHA-256）。
  一般（自分の申請のみ）と管理者（全件・承認・ユーザー管理）の2ロール。
  申請者名・承認者名はサーバー側でセッションから強制する。
- **個人ダッシュボード**: 自分の申請一覧・月次集計・パスワード変更。
- **管理者ダッシュボード**: 承認／却下、集計、検索、領収書あり／なしと運賃照合の
  絞り込み、CSV書き出し、ユーザー管理、事業部マスタ、運賃マスタ、
  AI解析の学習データの確認。
- **データの保管**: 申請データは Google スプレッドシート（タブ名・見出しは日本語。
  内部キーとJSON APIのキー名は英語で固定）、領収書画像は Google ドライブに
  事業部／年月で自動整理して保存する。
- **オフライン耐性**: 通信に失敗した申請は端末内のキューに退避し、「再同期」で反映する。

### 運用基盤

- **フロントエンドの自動デプロイ**: `main` への push で GitHub Pages へ公開する。
- **バックエンドの自動デプロイ**: `main` への push で clasp を使って Apps Script
  プロジェクトへ反映し、既存デプロイを新バージョンへ更新する。ウェブアプリURL
  （`.../exec`）は変わらないため、アプリ側の設定はやり直さなくてよい。
  マニフェスト（公開設定・タイムゾーン・OAuthスコープ）は取得したものを
  そのまま押し戻すので、CIが書き換えることはない。
- **デプロイ後の疎通確認とロールバック**: 公開されたウェブアプリに `status` を
  投げ、JSONが返らなければ直前のバージョンへ自動で戻す。
- **回帰テスト25件**: Google のサービスをスタブして依存なしで実行できる
  （`node apps-script/tests/memory.test.js`）。

### 既知の制約

- **Google検索グラウンディングは Gemini API の無料枠では使えない**（割当が0で、
  HTTP 429 に `limit: 0` が入る。モデルを変えても解決しない）。そのため運賃の
  Web照合は既定で無効で、運賃マスタによる照合が本線。課金を有効にした場合のみ
  スクリプトプロパティ `FARE_WEB_LOOKUP` に `true` を設定する。
- **路線検索サイトの自動読み取りは実装していない。** Google・Yahoo!路線情報・
  ジョルダン・NAVITIME はいずれも自動アクセスやデータ収集を規約で禁止している
  ため、人がクリックして開くリンクとして提供している。
- スクリプトプロパティ（APIキー等）はCIから操作できないため、Apps Script
  エディタで設定する。

### 残っている作業

- 運賃マスタへ、よく使う区間の片道運賃を登録する。管理者ダッシュボードの
  「まとめて登録」に、1行 `出発駅, 到着駅, 片道運賃[, 経路]` で貼り付けられる。
