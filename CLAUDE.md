# 経費申請アプリ

レシート画像のAI読み取りと承認ダッシュボードを備えた経費申請アプリ。
フロントエンドは GitHub Pages の静的サイト、バックエンドは Google Apps Script
Web App で、データはスプレッドシート、領収書画像は Google ドライブに入る。

公開URL: <https://km1121-design.github.io/expense-app/>

## 構成

```
expense-app/     アプリ本体（index.html / script.js / style.css。バニラJS・ビルドなし）
apps-script/
  Code.gs        バックエンド全体（doGet / doPost。これ1ファイル）
  tests/         Code.gs の回帰テスト（Google のサービスをスタブして実行）
  tools/         自動デプロイ用のスクリプト
  README.md      セットアップ・運用手順（利用者向け）
docs/
  expense-app-spec.md   仕様書
```

## 開発の進め方

- `main` へ直接 push しない。作業ブランチで進めて PR を作る。
- コード・コメント・ドキュメント・コミットメッセージは**日本語**で書く。
  スプレッドシートの見出しやUI文言も日本語。内部キーとJSON APIのキー名は英語で固定。
- 変更したら必ず回帰テストを流す。UIを変えたときはブラウザでも確認する。

## 確認コマンド

```bash
# 回帰テスト（依存なし。Google のサービスはスタブ済み）
node apps-script/tests/memory.test.js

# Code.gs の構文チェック。.gs は node --check にそのまま渡せないので拡張子を変える
cp apps-script/Code.gs /tmp/Code.js && node --check /tmp/Code.js
```

フロントエンドの確認は Playwright + プリインストールの Chromium
（`PLAYWRIGHT_BROWSERS_PATH` を使う。`playwright install` は実行しない）。
`file://` で `expense-app/index.html` を開き、`localStorage` に設定とセッションを
書き込み、`script.google.test` へのリクエストを `page.route` で差し替えれば、
バックエンドなしで一通り動かせる。

## デプロイ

画面とバックエンドは別の場所で動くので、経路が2つある。どちらも `main` への
push で自動実行される。

| 対象 | ワークフロー | 内容 |
| --- | --- | --- |
| `expense-app/` | `deploy-pages.yml` | GitHub Pages へ公開 |
| `apps-script/Code.gs` | `deploy-apps-script.yml` | clasp で Apps Script プロジェクトへ反映し、既存デプロイを新バージョンへ更新 |

バックエンド側の流れ:

1. 構文チェックと回帰テスト（失敗したらデプロイしない）
2. `clasp pull` で現在のプロジェクトを丸ごと取得
3. `tools/sync-source.mjs` でソースだけ差し替え（`appsscript.json` は取得したものを
   そのまま押し戻すので、公開設定がCIで書き換わらない）
4. `clasp push` → `clasp redeploy <既存デプロイID>`（ウェブアプリURLは変わらない）
5. `tools/smoke-test.mjs` で公開されたウェブアプリに `status` を投げて疎通確認。
   **失敗したら直前のバージョンへ自動でロールバックする**

必要なシークレットは登録済み（`CLASPRC_JSON` / `APPS_SCRIPT_ID` /
`APPS_SCRIPT_DEPLOYMENT_ID`）。認証が切れたら `clasp login` をやり直して
`CLASPRC_JSON` を更新する。手順は `apps-script/README.md` の「自動デプロイ」。

**スクリプトプロパティ（APIキー等）はCIから操作できない。** Apps Script API に
プロパティを触る口がないため、エディタの「プロジェクトの設定」で設定する。
CIで変えたい既定値はコード側に置く。

## 落とし穴

実際に事故になった項目。作業前に目を通すこと。

### Apps Script プロジェクトのスクリプトファイルは常に1つ

Apps Script は全スクリプトファイルを**同じスコープへ結合する**ため、同じコードが
2ファイルあると `const` の二重宣言でプロジェクト全体が SyntaxError になり、
全リクエストが落ちる（`Failed to fetch` になる）。

- **clasp はスクリプトを `.js` として取得する**（clasp v2 の既定は `.gs` だった）。
  このプロジェクトのファイル名は `コード` なので、ローカルには `コード.js` で降りてくる。
  `.gs` だけを探すと「ファイルなし」と誤判定して新規ファイルを作ってしまう。
- `tools/sync-source.mjs` が「スクリプトファイルは1つ」を保つ。反映先以外の
  スクリプトファイルは取り除き、内容の異なるファイルが複数あるときは中止する。
- **単体の構文チェックではこの不具合を検出できない**（結合して初めて壊れる）。
  デプロイ後の疎通確認が唯一の検出手段。

### スプレッドシートの列は「位置」で読み書きする

見出しは日本語の表示専用ラベル。列定義は `[内部キー, 日本語ラベル]` の組で、
読み書きは定義順の位置で行う。`headerKeys_` は日本語ラベルと旧英語名の
どちらからでも内部キーへ解決する。

タブ名・見出しの定義を変えたら `SCHEMA_VERSION` を更新する。次のリクエストで
全シートをまとめて移行する（`migrateSheets` はエディタから手動実行もできる）。
移行は `safeMigrateSheets_` で包まれており、失敗してもリクエストは落とさない。

### 起動の速さは「往復回数」で決まる（JSの実行は誤差）

計測すると、アプリ自身のJS実行は約200ms。残りはすべて外部待ちだった。

- **`index.html` に同期の `<script src="CDN">` を足さないこと。** `script.js` より前に
  置くと、その通信が終わるまでアプリが1行も動かない。Tesseract がこれで、
  AIキー設定時は一度も使わないのに毎回起動を待たせていた。いまは `ensureTesseract()`
  で使う直前に読み込む。
- **起動は `bootstrap` の1往復。** `status` + `me` + 一覧を1アクションで返す。
  画面とバックエンドは別々に配信されるため、`fetchBootstrap()` は
  `unknown action` を見たら従来の3往復へ自動で切り替える。**この経路を消さないこと。**
- **通信の前にキャッシュを描く。** クラウドモードでも先に `loadCache()` + `render()`
  してから同期する。白い画面を見せないための措置。
- **保存系のあとに一覧を取り直さない。** `create` は保存済みのレコードを返すので、
  それを `state.expenses` へ入れる。`refreshFromCloud()` を呼ぶと往復が倍になり、
  そのたびに全期間の申請をダウンロードすることになる。
- **1リクエスト内で同じシートを2度読まない。** `requireUser_` はユーザー行を
  読んでいるので `department` まで持たせてある（呼び出し側で `findUser_` を
  呼び直さない）。運賃マスタも、不在を確認済みなら `appendFare_` で追記する。

### 領収書画像はリンク共有にしている

画像はスクリプト所有者のドライブにあるため、共有しないと**所有者以外のブラウザからは
サムネイルの直リンクが403**になる。するとアプリは画像1枚ごとにバックエンドへ取得を
投げ直すことになり、一覧表示が目に見えて遅くなる（所有者本人だけは速いので気づけない）。

- `shareReceiptFile_` が保存時に「リンクを知っている人は閲覧可」にする。
  **URLを知っていれば認証なしで閲覧できる状態**である点は理解して使うこと。
  この扱いが許容できない運用では、この関数の中身を空にすれば従来の
  アプリ経由取得（遅いが所有者のみに閉じる）へ戻る。
- 共有設定を入れる前に保存された画像は、エディタで `shareExistingReceipts` を
  一度実行すると揃う。

### PL管理モデル連携: 日付が文字列だと SUMIFS は何も拾わない

`PL_SPREADSHEET_ID` を設定すると、承認済みの申請を別スプレッドシートの
『経費入力テーブル』へ反映する（PL計算シートの SUMIFS がそこを集計する）。

- **日付は必ず `Date` で書く。** PL側の SUMIFS は `">=2026-08-01"` と日付として
  比較するため、`"2026-08-05"` のような文字列を書くと**一件も一致せず全月 ¥0** に
  なる。連携前のモデルが全月 ¥0 だったのはこれが原因。`setupPlSheets` が既存の
  文字列日付を実日付へ移行する。
- **スクリプトとPLシートのタイムゾーンは違う。** スクリプトは Asia/Tokyo、PLモデルは
  America/Los_Angeles（Googleの既定）。`new Date(y, m, d)` はスクリプトTZの 0:00 なので、
  そのまま書くと**シート上では16時間戻って前日になる**。日付を書くときは必ず
  `toPlDate_(value, tz)` を使い、`tz` にシートのタイムゾーンを渡す。
- **日付の判定は必ずシートのタイムゾーンで行う。** シートの日付は**シートのTZ基準**で
  読み書きされる（読み出しにスクリプトのTZは使われない）。スクリプトのTZで
  「もう 0:00 か」を判定すると、**ずれた値がちょうど 0:00 に見えて素通りする**。
  実際にこれで復元処理が1件も対象を見つけられなかった。
- **月末の条件は `"<翌月1日"`。** `"<=2026-08-31"` だと、日付に時刻が残っている行は
  8月にも9月にも入らず**PLから消える**。`usePlHalfOpenRanges_` が半開区間へ直す。
- **手動入力行に触らないこと。** PL側の表はアプリ連携と手動入力が混在する設計。
  アプリ由来の行はG列『申請ID』で見分け、申請IDが空の行は読み書きしない。
  同じ申請は申請IDで上書きするため、何度同期しても重複しない。
- **語彙はコードに埋め込まない。** アプリ（交通費・本部）とPL（雑費交通費・
  イベント営業）で言葉が違う。変換は「PL連携マッピング」シートで管理する。
- **対応先の無い事業部は書かない。** 金額の行き先を勘で決めると別の事業部のPLを
  汚す。「PL連携スキップ」へ残して管理者ダッシュボードに警告を出す。科目のほうは
  「その他経費」という受け皿があるので落とさない。
- 「その他経費（実費）」行は**「事業部×月の経費合計 − 個別行で拾っている分」の差**で
  出している。科目名を並べた SUMIFS にしないのは、科目が増えたときに漏らさないため。
- PL側の見出しは**4行目**、データは**5行目**から。1行目はタイトル。

### フロントの `normalizeRecord` はホワイトリスト

`script.js` の `normalizeRecord` に無いフィールドは**黙って捨てられる**。
サーバーに項目を追加したら必ずここにも追加する。

### `style.css` の `[hidden]`

`.btn { display: inline-flex }` が `hidden` 属性を打ち消すため、
`[hidden] { display: none !important; }` を全体に効かせている。消さないこと。

### 通信は `text/plain` の POST

CORS のプリフライトを避けるため、JSON を `text/plain` で送っている。
`Content-Type: application/json` にすると動かなくなる。
サーバーがHTMLを返した場合（ログイン画面・エラー画面）は `readJsonResponse` が
読める日本語のエラーに変換する。

### 運賃のWeb照合と Gemini の無料枠

- **Google検索グラウンディングは無料枠では使えない。** 割当が0で、
  HTTP 429 に `limit: 0` が入って返る。モデルを変えても解決しない。
- そのため `FARE_WEB_LOOKUP` は**既定で無効**。`"true"` を設定したときだけ有効
  （課金が必要）。有効な状態で `limit: 0` を検知すると自動で `"false"` を保存する。
- `gemini-2.5-flash` は新規ユーザーには 404 を返す。モデルは `-latest` の別名を
  優先し、全滅したら `tryGeminiModels_` がそのキーで使えるモデルを調べて再試行する。
  エディタで `showGeminiModels` を実行すると一覧が出る。
- **路線検索サイト（Google / Yahoo!路線情報 / ジョルダン / NAVITIME）の自動読み取りは
  各社の規約で禁止されている。実装しない。** 人がクリックして開くリンクだけを提供する。

### AI解析の速さは出力トークン数で決まる

画像の縮小・`thinking` 無効化・`temperature:0`・`maxOutputTokens` はすでに入っている。
残る変数は**モデルに何行書かせるか**と**何回呼ぶか**の2つ。

- `raw_text` は抽出の根拠（グラウンディング）として必要だが、書かせるほど遅くなる。
  行数は `RECEIPT_RAW_TEXT_MAX_LINES` で調整する。精度が落ちたら増やす。
- **誤読履歴があるだけで読み直してはいけない。** よく使う店舗ほど毎回2回呼ぶことになる。
  `repeatsKnownMistake_` で「今回の読み取りが過去の誤読値と一致したとき」だけ読み直す。

### 運賃マスタは申請から育つ（最初の1件は誰も確認していない）

未登録の区間が申請されると、申請額から片道運賃を割り戻して運賃マスタへ登録する
（`resolveFareForRecord_`）。2回目以降の同じ区間は自動で照合される。

- **登録の元になった申請を `match` にしてはいけない。** 自分の申請額で自分を照合
  しても確認したことにならないため `registered`（バッジは「新規登録」）にしている。
  管理者はこのバッジが付いた申請の金額を承認前に確かめる。
- **既に登録済みの区間は上書きしない。** 上書きすると、申請者が申請額を変えるだけで
  照合の基準そのものを動かせてしまう。差は `diff` として出す。
- 割り切れない金額・`FARE_UNIT_MAX`（10万円）超・金額0は登録しない。
  桁の打ち間違いがマスタになると、以降の申請が誤った額で「一致」と判定される。

## 未完了の作業

いまのところなし。

- 運賃マスタへの区間の事前登録は不要になった（上記のとおり申請から貯まる）。
  よく使う区間を先に入れておきたい場合は、管理者ダッシュボードの「まとめて登録」
  （1行 `出発駅, 到着駅, 片道運賃[, 経路]`）が使える。

## ドキュメント

- 仕様: `docs/expense-app-spec.md`
- バックエンドの構築・運用: `apps-script/README.md`
- 利用者向けの概要: `README.md`
